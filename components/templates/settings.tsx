'use client'

// Types
import type { HomeProps } from '@/types/components/templates/home'

const SettingsTemplate = (props: HomeProps) => {
	// Props
	const { lng } = props

	return <>Settings</>
}

export default SettingsTemplate
